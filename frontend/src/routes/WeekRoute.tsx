import { RouteBand } from "./RouteBand";

export function WeekRoute() {
  return (
    <RouteBand title="Week" sub="the plan">
      <p className="text-base text-ink-soft">
        The seven-column proportional grid, its summary strip, its verdict, and the weekly session
        as a mode of this screen.
      </p>
    </RouteBand>
  );
}
