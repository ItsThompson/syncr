import { RouteBand } from "./RouteBand";

export function AreasRoute() {
  return (
    <RouteBand title="Areas" sub="wedges, floors, percentages">
      <p className="text-base text-ink-soft">
        Composition now, actual against target, and the pie review as a mode of this screen.
        Deviation rows carry no Area ink at all, including their label column.
      </p>
    </RouteBand>
  );
}
