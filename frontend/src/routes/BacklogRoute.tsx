import { RouteBand } from "./RouteBand";

export function BacklogRoute() {
  return (
    <RouteBand title="Backlog" sub="tasks and projects">
      <p className="text-base text-ink-soft">
        The table and its capture control. At-risk and overdue rows read differently from
        ordinary ones, from the same deadline arithmetic the verdict uses.
      </p>
    </RouteBand>
  );
}
