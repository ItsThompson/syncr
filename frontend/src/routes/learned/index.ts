/* The Learned screen's public face: the route, and nothing else.
 *
 * Its components, its figures and its signal table are internal to this directory. The route table imports the
 * route; nothing above this directory has a reason to reach a cell of it. */

export { LearnedRoute } from "./LearnedRoute";
