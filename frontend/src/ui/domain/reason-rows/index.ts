/* REASON ROWS: the labelled-row set a reason record and a definition list are both drawn as.
 *
 * The clause label column's width lives here, in `tokens.css`, promoted out of layer 1 with this component.
 */

export { ReasonRows, type ReasonRowsProps } from "./ReasonRows";
export type { LabelledRow } from "./row";
