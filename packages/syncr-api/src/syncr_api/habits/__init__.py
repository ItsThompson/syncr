"""The habits module: the entity, its five routes, and the two derivations rendered read-only.

A Habit answers *when and how often*, which is why roughly 30% of the blocks in a week come
from one. What this package adds over the domain package is persistence, authorization, and
the wire shapes; every invariant and both derivations live in ``syncr_domain``.
"""
