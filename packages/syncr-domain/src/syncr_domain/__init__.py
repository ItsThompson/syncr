"""Pure syncr domain: entities, invariants, and arithmetic. No I/O."""

# The interval algebra, zones, discretionary time, the feasibility probe, the
# rotation cursor, debt, and the plan value types land here in later slices. The
# package exists from the first commit so the dependency direction
# common -> domain -> solver -> api is real rather than aspirational.
