"""syncr placement. Depends on syncr-domain. Zero ML dependencies."""

# SolveInputs, materialize, the constraint checker, the objective, construction,
# local search, tie-breaking, and reason assembly land here in later slices. The
# package exists from the first commit so the zero-ML boundary is enforceable
# before there is any code to violate it.
