"""The syncr CLI: an OAuth client of the same API the browser uses."""

# The device flow, the command catalog, `--json`, and the exit-code contract land
# here in later slices. The package is a workspace member from the first commit so
# it never grows a dependency on syncr-api.
